---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: krocatalogs.kro.services.openmcp.cloud
spec:
  group: kro.services.openmcp.cloud
  names:
    kind: KROCatalog
    listKind: KROCatalogList
    plural: krocatalogs
    singular: krocatalog
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KROCatalog is a read-only catalog of available KRO versions.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the available KRO versions
            properties:
              versions:
                description: Available KRO versions.
                items:
                  description: KROCatalogVersion represents an available KRO version.
                  properties:
                    chartVersion:
                      description: ChartVersion is the helm chart version/tag (e.g.
                        "0.8.5").
                      type: string
                    version:
                      description: Version is the upstream application version (e.g.
                        "v0.8.5").
                      type: string
                  required:
                  - version
                  type: object
                type: array
            required:
            - versions
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
