---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: crossplanecatalogs.crossplane.services.opencp.cloud
spec:
  group: crossplane.services.opencp.cloud
  names:
    kind: CrossplaneCatalog
    listKind: CrossplaneCatalogList
    plural: crossplanecatalogs
    singular: crossplanecatalog
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CrossplaneCatalog is a read-only catalog of available Crossplane
          versions and providers.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the available Crossplane versions and providers
            properties:
              providers:
                description: Available Crossplane providers.
                items:
                  description: CrossplaneCatalogProvider represents an available Crossplane
                    provider with its versions.
                  properties:
                    name:
                      description: Name of the provider (e.g. "provider-kubernetes").
                      type: string
                    versions:
                      description: Available versions for this provider.
                      items:
                        type: string
                      type: array
                  required:
                  - name
                  - versions
                  type: object
                type: array
              versions:
                description: Available Crossplane versions.
                items:
                  description: CrossplaneCatalogVersion represents an available Crossplane
                    version.
                  properties:
                    version:
                      description: Version string (e.g. "v1.20.1").
                      type: string
                  required:
                  - version
                  type: object
                type: array
            required:
            - providers
            - versions
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
