apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: basic-graphql-gateway
  namespace: default
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: local-event-showcase
  path: ./demo/manifests/examples/kustomize/basic-graphql-gateway
  prune: true
  dependsOn:
    - name: basic-shoot
    - name: basic-kro
