apiVersion: initialization.kcp.io/v1alpha1
kind: InitTarget
metadata:
  name: openmcp-account-init
spec:
  workspaceTypeRef:
    name: opencp-init
    path: root
  sources:
    - template:
        name: openmcp-managed-control-plane
